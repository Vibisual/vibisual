/**
 * agent-harness — 집행(§5.11 v4.59).
 *
 * 이 카드가 재는 것을 **실제로 지키게** 하는 규칙. 켜 두면 이 프로젝트 에이전트의 매 턴 프롬프트에 실린다.
 *
 * ⚠ 둘째 줄은 원래 "권한을 늘려야 하면 승인을 받아라"였는데, 그것은 `least-privilege` 의 규칙과
 * 한 낱말 차이였다(어절 겹침 0.58). 두 카드를 함께 켜면 같은 지시가 매 턴 두 번 실려 무게만 나눠 가진다.
 * 승인 규율은 그 카드에 두고, 여기서는 이 카드 고유의 각도 — **무엇을 바꿔서 달라졌는지 남기는 것** — 을 맡는다.
 */
import { defineEnforcement } from '../sdk/index.js';

export const enforcement = defineEnforcement({
  id: "agent-harness",
  title: "하네스 우선",
  rules: [
    "결과가 나쁘면 모델부터 바꾸지 마라 — 도구·권한·격리·스킬·규칙을 먼저 점검하라.",
    "하네스를 손봐서 결과가 달라졌으면 무엇을 바꿔 달라졌는지 남겨라 — 다음에 같은 상황에서 처음부터 헤매지 않도록.",
  ],
});
